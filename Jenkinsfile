
pipeline()
{     
    environment {
        PATH="/opt/sap/neo-sdk/tools:$PATH"      
   }
   agent none
   stages{
   stage('initial')
   {
   steps
   {
     withEnv(['PATH+EXTRA=/usr/sbin:/usr/bin:/sbin:/bin'])
   echo "PATH=$PATH"  
}   
  
   }
   }
}
